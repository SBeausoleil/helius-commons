plugins {
    id 'java'
}

group = 'systems.helius'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
    testImplementation group: 'io.github.sbeausoleil', name: 'factorium', version: '2.1.0'
}

test {
    useJUnitPlatform()
}